# BOOTSTRAP.md Template

Bootstrap principles:
- Initialize with explicit assumptions.
- If assumptions are risky, downgrade confidence and request validation path.
- Prefer smallest viable plan that keeps pipeline moving.
- Persist only actionable state needed by downstream roles.
- Keep artifacts concise and machine-readable when possible.
