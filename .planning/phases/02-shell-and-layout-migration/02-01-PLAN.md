---
phase: 02-shell-and-layout-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - control-panel/frontend/src/components/layout/app-layout.tsx
  - control-panel/frontend/src/components/layout/sidebar.tsx
  - control-panel/frontend/src/components/layout/header.tsx
  - control-panel/frontend/src/app/page.tsx
autonomous: true
requirements:
  - UI-01
requirements_addressed:
  - UI-01
must_haves:
  truths:
    - application shell uses a Mosaic-aligned sidebar/header/content composition
    - layout primitives remain centralized in src/components/layout
    - dashboard entry route keeps AppLayout integration after shell migration
  artifacts:
    - control-panel/frontend/src/components/layout/app-layout.tsx
    - control-panel/frontend/src/components/layout/sidebar.tsx
    - control-panel/frontend/src/components/layout/header.tsx
    - control-panel/frontend/src/app/page.tsx
  key_links:
    - all non-login pages continue rendering inside AppLayout while using updated shell styles
---

# Phase 2 Plan 01: Rework Global App Shell to Mosaic Layout Pattern

<objective>
Migrate the shared shell structure (sidebar, header, content frame) to Mosaic-style layout conventions while preserving current application behavior.
</objective>

## Objective
- **What:** Rebuild shared layout composition for sidebar/header/content in a Mosaic-aligned structure.
- **Why:** Satisfy UI-01 and establish the visual/structural base for downstream UI modernization.
- **Output:** Updated `app-layout`, `sidebar`, and `header` components applied to dashboard shell.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@control-panel/frontend/src/components/layout/app-layout.tsx
@control-panel/frontend/src/components/layout/sidebar.tsx
@control-panel/frontend/src/components/layout/header.tsx
@control-panel/frontend/src/app/page.tsx

## Tasks

<task type="code">
  <name>Refactor shell frame for Mosaic-style information hierarchy</name>
  <files>control-panel/frontend/src/components/layout/app-layout.tsx
control-panel/frontend/src/components/layout/sidebar.tsx
control-panel/frontend/src/components/layout/header.tsx</files>
  <read_first>control-panel/frontend/src/components/layout/app-layout.tsx
control-panel/frontend/src/components/layout/sidebar.tsx
control-panel/frontend/src/components/layout/header.tsx
control-panel/frontend/src/app/layout.tsx</read_first>
  <action>Rework AppLayout to use a clearer Mosaic-like shell hierarchy: stable left navigation rail, top contextual header, and constrained content frame with consistent spacing tokens. Update sidebar and header visual contracts to align typography, border treatment, and section grouping while preserving current data hooks and auth-sensitive behavior.</action>
  <acceptance_criteria>1) Sidebar, header, and main content are represented as explicit shell regions in AppLayout
2) Sidebar navigation remains the single source of global route entry points
3) Header keeps route title and approval indicator behavior after style/layout refactor
4) Shell styling uses existing project theme variables (no hard-coded one-off palette drift)</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm typecheck</verify>
  <done>Shared shell layout follows Mosaic-like structure without breaking existing interactions.</done>
</task>

<task type="code">
  <name>Apply updated shell contract to dashboard entry page</name>
  <files>control-panel/frontend/src/app/page.tsx
control-panel/frontend/src/components/layout/app-layout.tsx</files>
  <read_first>control-panel/frontend/src/app/page.tsx
control-panel/frontend/src/components/layout/app-layout.tsx</read_first>
  <action>Adjust the dashboard page container usage so it matches the updated shell spacing and section rhythm. Preserve all dashboard data bindings and existing widgets while eliminating structural mismatches caused by shell changes.</action>
  <acceptance_criteria>1) Dashboard route (`/`) renders successfully inside updated AppLayout
2) No dashboard widget is dropped during layout integration
3) Main content area keeps vertical scroll behavior without clipping header/sidebar</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm build</verify>
  <done>Dashboard route is fully integrated with the new shell baseline.</done>
</task>

## Verification
Validate shell composition and dashboard integration by type-checking and production build after the layout refactor.

## Success Criteria
- [ ] App shell is structurally aligned with Mosaic-style sidebar/header/content layout
- [ ] Dashboard route consumes updated shell without data or interaction regressions
- [ ] No build/type errors introduced by shell migration
