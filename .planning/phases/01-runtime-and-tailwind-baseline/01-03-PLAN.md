---
phase: 01-runtime-and-tailwind-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - control-panel/frontend/package.json
  - control-panel/frontend/next.config.ts
  - control-panel/frontend/src/app/globals.css
autonomous: true
requirements:
  - QUAL-02
requirements_addressed:
  - QUAL-02
must_haves:
  truths:
    - frontend type-check passes after baseline dependency and css updates
    - frontend production build passes after baseline dependency and css updates
    - no unresolved module, typing, or css pipeline errors remain
  artifacts:
    - control-panel/frontend/package.json
    - control-panel/frontend/next.config.ts
    - control-panel/frontend/src/app/globals.css
  key_links:
    - successful pnpm typecheck and pnpm build validate baseline quality gate
---

# Phase 1 Plan 03: Run Type-Check and Production Build Quality Gate

<objective>
Validate that runtime and Tailwind baseline changes still produce a type-safe and buildable frontend artifact.
</objective>

## Objective
- **What:** Execute and enforce frontend type-check and production build gates.
- **Why:** Satisfy QUAL-02 before moving into template integration phases.
- **Output:** Passing type-check and build in control-panel/frontend with no baseline regressions.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@control-panel/frontend/package.json
@control-panel/frontend/next.config.ts
@control-panel/frontend/src/app/globals.css

## Tasks

<task type="code">
  <name>Run TypeScript quality gate and fix baseline regressions</name>
  <files>control-panel/frontend/package.json
control-panel/frontend/src</files>
  <read_first>control-panel/frontend/package.json
control-panel/frontend/tsconfig.json</read_first>
  <action>Run "pnpm typecheck" from control-panel/frontend and resolve only errors introduced or surfaced by the baseline updates, keeping changes minimal and constrained to type or import compatibility fixes.</action>
  <acceptance_criteria>1) pnpm typecheck exits with code 0
2) fixes do not change product behavior outside compatibility scope
3) no new lint or type suppressions are added without rationale in diff</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm typecheck</verify>
  <done>Type-level integrity is restored and validated after baseline updates.</done>
</task>

<task type="code">
  <name>Run production build gate and close remaining blockers</name>
  <files>control-panel/frontend/next.config.ts
control-panel/frontend/src</files>
  <read_first>control-panel/frontend/next.config.ts
control-panel/frontend/src/app/globals.css</read_first>
  <action>Run "pnpm build" from control-panel/frontend and resolve any remaining runtime, bundling, or css pipeline errors that block build completion, preserving current route structure and app behavior.</action>
  <acceptance_criteria>1) pnpm build exits with code 0
2) no unresolved import/module/css errors are present in build output
3) resulting changes remain within baseline stabilization scope</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm build</verify>
  <done>Production build finishes successfully and baseline quality gate is closed.</done>
</task>

## Verification
Treat type-check and build as hard gates; phase 1 is not complete until both commands pass.

## Success Criteria
- [ ] `pnpm typecheck` passes in control-panel/frontend
- [ ] `pnpm build` passes in control-panel/frontend
- [ ] baseline updates are validated without introducing route regressions
