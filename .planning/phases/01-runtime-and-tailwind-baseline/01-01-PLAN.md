---
phase: 01-runtime-and-tailwind-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - control-panel/frontend/package.json
  - control-panel/frontend/pnpm-lock.yaml
autonomous: true
requirements:
  - PLAT-01
requirements_addressed:
  - PLAT-01
must_haves:
  truths:
    - control-panel/frontend/package.json pins next and eslint-config-next to 16.2.2
    - lockfile reflects updated Next.js runtime dependencies
  artifacts:
    - control-panel/frontend/package.json
    - control-panel/frontend/pnpm-lock.yaml
  key_links:
    - next@16.2.2 in package.json is resolved in pnpm-lock.yaml
---

# Phase 1 Plan 01: Upgrade Next.js Runtime to 16.2.2

<objective>
Upgrade the frontend runtime dependencies to Next.js 16.2.2 safely, including lockfile synchronization.
</objective>

## Objective
- **What:** Raise Next.js runtime packages from 16.2.0 to 16.2.2.
- **Why:** Align runtime baseline with phase requirement PLAT-01.
- **Output:** Updated dependency manifests with deterministic lockfile.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@control-panel/frontend/package.json

## Tasks

<task type="code">
  <name>Upgrade Next.js and aligned frontend package versions</name>
  <files>control-panel/frontend/package.json
control-panel/frontend/pnpm-lock.yaml</files>
  <read_first>control-panel/frontend/package.json
control-panel/frontend/pnpm-lock.yaml</read_first>
  <action>Set "next" to "16.2.2" under dependencies and set "eslint-config-next" to "16.2.2" under devDependencies in control-panel/frontend/package.json, then run "pnpm install --lockfile-only" from control-panel/frontend to refresh pnpm-lock.yaml without unrelated dependency drift.</action>
  <acceptance_criteria>1) control-panel/frontend/package.json contains "\"next\": \"16.2.2\""
2) control-panel/frontend/package.json contains "\"eslint-config-next\": \"16.2.2\""
3) control-panel/frontend/pnpm-lock.yaml contains "/next@16.2.2" at least once
4) git diff for package.json changes only targeted Next.js version lines and keeps other dependency versions unchanged</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm install --lockfile-only</verify>
  <done>Dependency manifest and lockfile both reflect Next.js 16.2.2 baseline.</done>
</task>

<task type="code">
  <name>Run focused dependency integrity checks after upgrade</name>
  <files>control-panel/frontend/package.json
control-panel/frontend/pnpm-lock.yaml</files>
  <read_first>control-panel/frontend/package.json
control-panel/frontend/pnpm-lock.yaml</read_first>
  <action>Run "pnpm list next eslint-config-next --depth 0" from control-panel/frontend and confirm resolved top-level versions are exactly 16.2.2 for both packages; if not, adjust lockfile by re-running lockfile update and re-check.</action>
  <acceptance_criteria>1) command output includes "next 16.2.2"
2) command output includes "eslint-config-next 16.2.2"
3) no extra package.json changes are introduced during integrity check</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm list next eslint-config-next --depth 0</verify>
  <done>Runtime upgrade is pinned and verified before subsequent phase plans execute.</done>
</task>

## Verification
Confirm dependency upgrade is fully applied and lockfile-resolved before touching styling or build gates.

## Success Criteria
- [ ] Next.js version is 16.2.2 in package manifest
- [ ] eslint-config-next version is 16.2.2 in package manifest
- [ ] Lockfile resolves next@16.2.2
