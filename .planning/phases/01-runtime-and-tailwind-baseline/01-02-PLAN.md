---
phase: 01-runtime-and-tailwind-baseline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - control-panel/frontend/package.json
  - control-panel/frontend/postcss.config.mjs
  - control-panel/frontend/src/app/globals.css
autonomous: true
requirements:
  - PLAT-02
requirements_addressed:
  - PLAT-02
must_haves:
  truths:
    - tailwindcss dependency remains on a v4.2.x version
    - postcss config keeps @tailwindcss/postcss configured for Next.js
    - global stylesheet retains the tailwindcss import entry point
  artifacts:
    - control-panel/frontend/package.json
    - control-panel/frontend/postcss.config.mjs
    - control-panel/frontend/src/app/globals.css
  key_links:
    - postcss.config.mjs plugin declaration and globals.css import model match Tailwind v4 setup
---

# Phase 1 Plan 02: Validate Tailwind v4.2 Baseline on Next.js

<objective>
Ensure Tailwind CSS stays on v4.2.x and matches the official Next.js integration pattern before template migration.
</objective>

## Objective
- **What:** Confirm and align Tailwind CSS baseline configuration in the frontend app.
- **Why:** Satisfy PLAT-02 and prevent styling pipeline drift before dashboard migration.
- **Output:** Tailwind v4.2-compatible package, PostCSS plugin, and global CSS import model.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@control-panel/frontend/package.json
@control-panel/frontend/postcss.config.mjs
@control-panel/frontend/src/app/globals.css

## Tasks

<task type="code">
  <name>Enforce Tailwind v4.2 package and PostCSS plugin baseline</name>
  <files>control-panel/frontend/package.json
control-panel/frontend/postcss.config.mjs</files>
  <read_first>control-panel/frontend/package.json
control-panel/frontend/postcss.config.mjs</read_first>
  <action>Ensure "tailwindcss" stays on "4.2.x" and "@tailwindcss/postcss" remains declared in package dependencies, then keep postcss.config.mjs wired with the "@tailwindcss/postcss" plugin entry for Next.js compilation.</action>
  <acceptance_criteria>1) control-panel/frontend/package.json keeps tailwindcss on version 4.2.x
2) control-panel/frontend/package.json includes "@tailwindcss/postcss"
3) control-panel/frontend/postcss.config.mjs references "@tailwindcss/postcss" exactly once as active plugin</acceptance_criteria>
  <verify>cd control-panel/frontend && pnpm list tailwindcss @tailwindcss/postcss --depth 0</verify>
  <done>Tailwind dependency and PostCSS bridge remain aligned to v4 baseline.</done>
</task>

<task type="code">
  <name>Validate global Tailwind import entry model</name>
  <files>control-panel/frontend/src/app/globals.css
control-panel/frontend/postcss.config.mjs</files>
  <read_first>control-panel/frontend/src/app/globals.css
control-panel/frontend/postcss.config.mjs</read_first>
  <action>Keep src/app/globals.css importing "tailwindcss" and preserve any existing app-specific layers, then verify both globals.css and postcss.config.mjs still expose a coherent Tailwind v4 pipeline without removing existing theme or shadcn additions.</action>
  <acceptance_criteria>1) src/app/globals.css contains "@import \"tailwindcss\";"
2) existing app-specific stylesheet imports are preserved
3) configuration and stylesheet checks pass in one scriptable verification command</acceptance_criteria>
  <verify>cd control-panel/frontend && node -e "const fs=require('fs');const postcss=fs.readFileSync('postcss.config.mjs','utf8');const css=fs.readFileSync('src/app/globals.css','utf8');if(!postcss.includes('@tailwindcss/postcss')||!css.includes('tailwindcss')){throw new Error('Tailwind baseline missing')}console.log('Tailwind baseline OK')"</verify>
  <done>Tailwind global import model is explicitly validated for the App Router entry stylesheet.</done>
</task>

## Verification
Confirm the CSS pipeline foundation is stable and consistent with Tailwind v4 + Next.js before UI migration work.

## Success Criteria
- [ ] Tailwind package remains on v4.2.x in frontend dependencies
- [ ] PostCSS config contains @tailwindcss/postcss integration
- [ ] globals.css keeps the tailwindcss import entry point without regressions
