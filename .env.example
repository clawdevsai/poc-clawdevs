# ClawDevs — exemplo de variáveis de ambiente
# Copie para .env e preencha: cp .env.example .env
# Nunca commitar .env com valores reais.

# --- Telegram (interface Diretor ↔ CEO; só o CEO usa) ---
# Bot token: criar em https://t.me/BotFather com /newbot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Slack (todos os agentes podem conversar; discussões entre agentes usam Ollama local GPU) ---
# App Token (xapp-...) e Bot Token (xoxb-...): criar app em https://api.slack.com/apps, workspace clawdevsai, Socket Mode.
# Ver: https://docs.openclaw.ai/channels/slack
SLACK_ENABLED=true
SLACK_APP_TOKEN=
SLACK_BOT_TOKEN=
# Opcional: ID do Diretor no Slack (allowFrom). Workspace ClawDevsAi: clawdevsai = Primary Owner.
SLACK_DIRECTOR_USER_ID=UOAHL10QK7D
# Outros membros que podem mandar DM ao bot (Member ID no Manage members), separados por vírgula.
# ClawDevsAi: Diego Morais (UDAJLLSDWZU), lukewaresoftwarehouse (UDAHPOA1M59). Se vazio e DIRECTOR também, usa pairing.
SLACK_ALLOWED_USER_IDS=UDAJLLSDWZU,UDAHPOA1M59
# Depuração: OPENCLAW_LOG=1 grava log em openclaw-gateway.log (tail -f openclaw-gateway.log)
# OPENCLAW_LOG=1

# --- Ollama (quando rodar OpenClaw fora do cluster) ---
OLLAMA_HOST=http://127.0.0.1:11434

# --- Opcional: Google Gemini (CEO em nuvem em fases futuras) ---
# GOOGLE_AI_KEY=
