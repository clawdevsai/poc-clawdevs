apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base

secretGenerator:
  - name: openclaw-auth
    envs:
      - .env
  - name: ollama-auth
    envs:
      - .env
  - name: clawdevs-panel-auth
    envs:
      - .env

generatorOptions:
  disableNameSuffixHash: true
