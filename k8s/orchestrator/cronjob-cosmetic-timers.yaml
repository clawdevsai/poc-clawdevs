# CronJob: check-timers de aprovação por omissão cosmética (Fase 3 — 033).
# Requer orchestrator-scripts e orchestrator-env.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cosmetic-timers
  namespace: ai-agents
  labels:
    app: orchestrator
spec:
  schedule: "*/10 * * * *"  # a cada 10 min
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: cosmetic
              image: python:3.12-slim
              command:
                - /bin/sh
                - -c
                - "pip install -q redis && cd /scripts && python cosmetic_omission.py check-timers && python cosmetic_omission.py write-qa-file"
              envFrom:
                - configMapRef:
                    name: orchestrator-env
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
                  readOnly: true
                - name: data
                  mountPath: /data/agents-devs
          volumes:
            - name: scripts
              configMap:
                name: orchestrator-scripts
            - name: data
              emptyDir: {}
