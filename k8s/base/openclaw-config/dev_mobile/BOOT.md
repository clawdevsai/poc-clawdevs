# BOOT.md - Dev_Mobile

Ao iniciar:
1. Carregar `IDENTITY.md`.
2. Carregar `AGENTS.md`.
3. Ler `README.md` do repositório para entender a aplicação, stack e plataformas alvo.
4. Carregar `SOUL.md`.
5. Carregar `INPUT_SCHEMA.json`.
6. Validar `/data/openclaw` e workspace de implementação.
7. Detectar framework mobile pelo `technology_stack` da task ou por arquivos do projeto (`app.json`, `expo.json`, `pubspec.yaml`).
8. Identificar plataforma alvo: iOS, Android ou ambas.
9. Carregar comandos padrão por framework (Expo/EAS ou Flutter).
10. Validar ferramentas no PATH: `node`, `npm`, `npx`, `expo`, `eas-cli` (ou `flutter`, `dart`).
11. Verificar presença de artefatos UX (`/data/openclaw/backlog/ux/`) para tasks com escopo de tela.
12. Pronto para receber task do Arquiteto.
