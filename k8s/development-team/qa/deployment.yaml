# Pod QA (Fase 1 — 014). Consome review_architect_done, publica review_qa_done.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa-slot
  namespace: ai-agents
  labels:
    app: qa-slot
spec:
  replicas: 0
  selector:
    matchLabels:
      app: qa-slot
  template:
    metadata:
      labels:
        app: qa-slot
    spec:
      containers:
        - name: slot
          image: python:3.12-slim
          command:
            - /bin/sh
            - -c
            - "pip install -q redis && cd /config && exec python slot_agent_single.py"
          envFrom:
            - configMapRef:
                name: qa-slot-env
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          volumeMounts:
            - name: scripts
              mountPath: /config
              readOnly: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: scripts
          configMap:
            name: agent-slot-scripts
            optional: true
---
apiVersion: v1
kind: Service
metadata:
  name: qa-slot-service
  namespace: ai-agents
  labels:
    app: qa-slot
spec:
  selector:
    app: qa-slot
  ports: []
  type: ClusterIP
