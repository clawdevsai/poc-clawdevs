# requirements.base.txt — ClawDevs
# Dependências base para todos os agentes.
# Modelos LLM NÃO estão aqui; ficam em volumes PVC.
# Referência: docs/04-infraestrutura.md (Docker multi-stage)

# Redis (Event Bus, GPU Lock, estado global)
redis>=5.0.0

# HTTP para chamadas Ollama e APIs externas
requests>=2.31.0
httpx>=0.27.0

# YAML para configuração
pyyaml>=6.0.1

# Utilitários
python-dotenv>=1.0.0

# Segurança: análise de entropia e hashing
# (semgrep instalado separadamente na imagem CyberSec)

# Logging estruturado
structlog>=24.1.0

# LanceDB para persistência de estado (cluster acéfalo, FreeRide)
lancedb>=0.6.0

# Tqdm para progress bars em scripts
tqdm>=4.66.0
